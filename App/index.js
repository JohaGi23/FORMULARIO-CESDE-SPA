// Importa la función App desde el archivo App.js
import { App } from "./App.js";
// Selecciona el objeto document
const info = document;
// Agrega un evento que se dispare cuando el contenido HTML haya sido completamente cargado y parseado
info. addEventListener("DOMContentLoaded", App);
